<%= form_for(@type_course) do |f| %>
  <% if @type_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_course.errors.count, "error") %> prohibited this type_course from being saved:</h2>

      <ul>
      <% @type_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
